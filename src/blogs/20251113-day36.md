#### 哦真是酣畅淋漓的python后端开发

直接用飞书lark_oapi库，没多少样板代码
但是进行了艰难的debug
组长和一位员工也协助了我一段时间，最后也是成功把Agent接到飞书上了
python开发还挺省事的，两天就做完了，头发也保住了

中间遇到两个问题
一个是飞书机器人要求3秒返回，否则报超时，但是调用一次要五六分钟处理
另一个是不能允许用户在这五六分钟内多次调用。
前一个问题很好解决，用multiprocessing库创建子进程异步处理即可
后一个问题需要维护一个用户ID到子进程对象的表（字典），用threading库保证创建子进程的逻辑线程安全就行了

python并发编程知识+2
