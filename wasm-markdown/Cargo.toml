[package]
name = "wasm-markdown"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
pulldown-cmark = { version = "0.12", default-features = false, features = ["simd", "html"] }
regex = "1"

[profile.release]
opt-level = "s"
lto = true
